/**
 * Set on the agent dashboard root (`style={{ [AGENT_APP_HEADER_HEIGHT_VAR]: "Npx" }}`).
 * The ticket drawer uses the same name for `top` / `height` so it sits below the header.
 */
export const AGENT_APP_HEADER_HEIGHT_VAR = "--agent-app-header-h";
