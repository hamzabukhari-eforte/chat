# Local development only: SES HTTP API host (REST + media). Production uses the site origin.
# NEXT_PUBLIC_HTTP_API_ORIGIN=http://10.0.10.53:8080
#
# WebSocket host/path: CHAT_WS_HOST / CHAT_WS_PATH in hooks/useWebSocketChat.ts
